import {detailsActions} from './detailsActions/constants';
import {homeActions} from './homeActions/constants';
import {loaderActions} from './loader/constants';

export const ACTIONS = {
  HOME: homeActions,
  DETAILS: detailsActions,
  LOADER: loaderActions,
};
